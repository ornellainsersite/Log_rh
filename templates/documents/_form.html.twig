{{ form_start(form) }}
    {{ form_widget(form) }}
    <h3>Titre<h3>
    < {{ form_start(form) }}
        {{ form_label(form.title) }}
        {{ form_widget(form.title) }}
        {{ form_rest(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}