{# {{ form_start(form) }}
    {{ form_widget(form) }}
    <h3>Titre<h3>
    <input type="text">{{ document.title }}</input>
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

 #}

 <div class="container">
        <h2>Créer un sujet</h2>
            <div class="inputBox">
            {{ form_start(form) }}
            {{ form_row(form.title, {'attr': {'placeholder': 'Sujet...'}})}}
            </div>
            <div class="inputBox">
                <input type="submit" value="Créer">
                {{ form_end(form) }} 
            </div>
    </div>
